# Simple Python program to calculate the square of a number

def square_number():
    number = input("Enter a number to square: ")
    squared_number = int(number) ** 2
    print(f"The square of {number} is {squared_number}.")
#It seems like the only issue that can come from the input is a value error
#if they put in a string or float, the code will fail. 
try:
    square_number()
except ValueError:
    print("""
          Sorry, maybe my instructions weren't clear.
          Try just an integer. Nothing else.
          """)
except:
    print("""
          I don't know what you did, but that isn't right...
          """)



